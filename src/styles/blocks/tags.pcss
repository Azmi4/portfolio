.tags {
  display: flex;
}

.tags__item {
  background: #f3f5f9;
  padding: 18px 20px;
  margin-right: 10px;
  border-radius: 30px;
  font-size: 15px;
  font-weight: 600;
  text-align: center;

  &:last-child {
    margin-right: 0;
  }
}
